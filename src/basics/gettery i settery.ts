// metody zaczynające się słowem kluczowym get to gettery odwołujemy się do nich jak do zwykłych pól. Wazne jest, ze gettery są tylko do odczytu wiec nie mozemy im przypisywac innych wartosci

//get lifeEvents(): string [] {
    //return this.lifeEventsList.filter(s => s !== 'Kompromitacja');}
 //const bartek9= new Human ('Bartek', 'B.');

 //Settery pozwalją zapisywać (ustawiać) nowe wartości.